<!DOCTYPE html>
<html lang="ko">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <style>
      * {
        margin: 0;
        padding: 0;
      }
      html {
        width: 100vw;
        height: 100vh;
      }
      body {
        width: 100%;
        height: 100%;
      }
      header {
        padding: 1.2em;
        text-align: center;
        background-color: #f2aa4c;
        color: #000000;
      }
      form,
      table {
        width: 80%;
        margin: 20px auto;
      }
      th {
        padding: 5px;
        font-size: 20px;
      }
      td,
      th {
        border-bottom: 1px solid #bbb;
        border-top: 1px solid #bbb;
      }
      input {
        border-radius: 3px;
        padding: 10px;
        outline: none;
      }
    </style>
  </head>
  <body>
    <header>
      <h1>학생정보</h1>
    </header>
    <form method="POST">
      <label>학생이름</label>
      <input name="st_name" />
    </form>
    <button onClick="location.href='http://localhost:3000/'">전체보기</button>
    <button onClick="location.href='http://localhost:3000/list'">추가</button>
    <table>
      <thead>
        <tr>
          <th>학번</th>
          <th>이름</th>
          <th>학과</th>
          <th>학년</th>
          <th>전화번호</th>
          <th>주소</th>
        </tr>
      </thead>
      <tbody>
        <% schoolData.forEach((students)=> { %>
        <tr>
          <td><%= students.st_num %></td>
          <td><%= students.st_name %></td>
          <td><%= students.st_dept %></td>
          <td><%= students.st_grade %></td>
          <td><%= students.st_tel %></td>
          <td><%= students.st_addr %></td>
        </tr>
        <% }) %>
      </tbody>
    </table>
  </body>
  <script></script>
</html>
